<view wx:if="{{name && age}}" style='text-align:center'>
  <text decode="{{true}}">&ensp;</text> 姓名:{{ name }}，年龄:{{ age }}
  <text decode="{{true}}">&ensp;</text>
</view>
<button bindtap="sendRequest">发送请求</button>